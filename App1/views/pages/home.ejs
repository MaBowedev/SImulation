<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="output.css">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>
<body>


    
    <section>

        
        <div class="bg-gray-300  p-2 h-screen ">
           <header class="bg-sky-300 p-2 w-full flex justify-center"> 
             <img class="h-10 w-10" src="/images/Tweet.png" alt="">
            <h1 class="text-2xl font-extrabold mb-5">Tweetto</h1>
        </header>
            <div class="flex flex-col gap-6 items-center" id="container">
                <% messages.forEach(msg => {%>
                   <div class="border bg-emerald-200 mt-5 p-1 border-black rounded-md w-1/6">
                        <p><%= msg.text %></p>
                        <p><%= msg.date %></p>
                       <div class="flex gap-1">
                            <button class="border border-black rounded-md">Edit</button>
                            <button class="border border-black rounded-md">Save</button>
                            <button class="border border-black rounded-md">Delete</button>
                       </div>
                          
                   </div>
                
            <% }) %></div>
        <div class="flex justify-center">
            <form id="form" action="/send" method="POST">
                <input  id="text" name="text" placeholder="digita max 150 caratteri" type="text">
                <button class="bg-blue-300 p-1 mt-2" id="btn">Tweet</button>
            </form>
        </div>
    </div>

    </section>
    
</body>
</html>